[project]
name = "mm-geoip-mcp"
version = "0.1.0"
description = "An MCP server that queries MaxMind mmdb databases"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.10.5",
    "geoip2>=5.1.0",
]
